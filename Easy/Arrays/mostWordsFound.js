const sentences = ["alice and bob love leetcode", "i think so too", "this is great thanks very much"]

function mostWordsFound(sentences) {
    // let result = []
    // // loop through the sentence
    // sentences.forEach(sentence => {
    //     // each sentence split the sentence between _____ .
    //     let splitedSentence = sentence.split(" ").length
    //     console.log(splitedSentence)
    //     result.push(splitedSentence)
    // });

    // return Math.max(...result.map(elment => elment.length))
}
        
console.log(mostWordsFound(sentences))
    